#include <stdlib.h>
#include <dog.h>

/**
 * dog - struct data type
 * @p: pointer to struct
 * @name: name
 * @age: age
 * @owner: owner
 */

void init_dog(struct dog *d, char *name, float age, char *owner)
{
	if (p == NULL)
		p = sizeof(struct dog);
	p.name = name;
	p.age = age;
	p.owner = owner;
}
